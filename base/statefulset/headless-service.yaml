apiVersion: v1
kind: Service
metadata:
  name: postgres # must match serviceName in StatefulSet
spec:
  clusterIP: None # headless gives each pod its own stable DNS
  selector: { app: postgres }
  ports:
    - port: 5432
